{% extends "base.html.twig" %}

{% block body %}
    <h1>{{ welcome }}</h1>

    <a href="{{ path('program_index') }}">
        Voir les séries
    </a>
    </br>
    <a href="{{ path('category_index') }}">
        Voir les catégories
    </a>

{% endblock %}