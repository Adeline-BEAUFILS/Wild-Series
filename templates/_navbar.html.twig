<nav class="navbar navbar-expand-lg navbar-dark bg-none">
  <a class="navbar-brand" href="/">Wild-Series</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/"> <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('program_index') }}">Series</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('category_index') }}">Categories</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('episode_index') }}">Episodes</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('actor_index') }}">Acteurs</a>
      </li>
      <li class="nav-item">
        {% if app.user %}
          
           {{ app.user.email }} <a class="nav-link" href="{{ path('app_logout') }}">  Se déconnecter</a>
        {% else %}
            <a class="nav-link" href="{{ path('app_login') }}"> Se connecter</a>
        {% endif %}
      </li>  
    </ul>
  </div>
</nav>